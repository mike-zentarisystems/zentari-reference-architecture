# Compose

Reserved for pinned, environment-scoped application stacks. Each stack must include example configuration, health checks, persistent-volume ownership, backup hooks, resource limits, logging, private/public network intent, upgrade steps, and rollback.
