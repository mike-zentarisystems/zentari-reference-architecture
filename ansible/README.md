# Ansible

Reserved for idempotent host configuration after M0 inventory and Tailscale/IAM design are approved. Inventories must separate production, AI, staging, infrastructure, and watchtower groups; secrets use an approved vault, never plaintext files.
