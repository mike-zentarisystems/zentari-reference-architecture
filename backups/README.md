# Backup automation

Reserved for backup definitions and non-secret configuration. Repositories, credentials, passwords, raw database dumps, and recovery artifacts are excluded from Git. Every job must define consistency method, retention, monitoring, restore procedure, and test cadence.
