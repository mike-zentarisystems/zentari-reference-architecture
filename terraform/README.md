# Terraform

Reserved for provider-managed resources such as DNS, cloud instances, buckets, and access policy after import and state-backend decisions are recorded. Existing resources must be imported and reviewed before apply; state and credentials never enter Git.
