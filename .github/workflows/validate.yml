name: Validate reference architecture

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  docs:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate documentation and inventories
        shell: pwsh
        run: ./scripts/validate-docs.ps1
